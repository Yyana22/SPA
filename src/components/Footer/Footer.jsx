import React from 'react';
import classes from '../Footer/Footer.module.css'

const Footer = () => {
    return (
        <footer className={classes.footer}>FFFFFFFFF</footer>
    )
}

export default Footer;